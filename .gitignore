# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
/node_modules/

# Build artifacts
/dist/
/dist-ssr/
/build/ # Common alternative for build output

# Environment variables
.env
.env.*.local # For local environment overrides (e.g., .env.development.local, .env.production.local)
.env.local

# Local development files
.vite/ # If Vite creates a cache/temp directory
.parcel-cache/ # If using Parcel bundler
.next/ # If using Next.js
.nuxt/ # If using Nuxt.js
.svelte-kit/ # If using SvelteKit

# IDE and OS specific files
.vscode/
!.vscode/extensions.json # Keep extensions.json to recommend extensions
.idea/ # JetBrains IDEs (IntelliJ, WebStorm, etc.)
.DS_Store # macOS
Thumbs.db # Windows
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.bak # Backup files from some editors

# Uploaded files (if your application handles user uploads)
/uploads/